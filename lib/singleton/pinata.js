// "server only";

// import { PinataSDK } from "pinata";

// const globalForPinata = globalThis;
// const pinata =
//   globalForPinata.pinata ??
//   new PinataSDK({
//     pinataJwt: `${process.env.PINATA_JWT}`,
//     pinataGateway: `${process.env.NEXT_PUBLIC_GATEWAY_URL}`,
//   });
// if (process.env.NODE_ENV !== "production") globalForPinata.pinata = pinata;

// export { pinata };
