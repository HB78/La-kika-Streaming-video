export const metadata = {
  title: "Creation d'une nouvelle serie",
  description: "Page de création de serie pour les administrateurs",
};

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  );
}
